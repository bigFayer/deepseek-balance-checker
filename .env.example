# 环境变量配置示例
# 复制此文件为 .env 并填入实际值

# 服务器端口 (可选，默认为 3000)
PORT=3000

# 运行环境 (development 或 production)
NODE_ENV=development

# 生产环境允许的源 (可选)
ALLOWED_ORIGIN=https://your-domain.com

# DeepSeek API 配置 (可选，用于测试)
DEEPSEEK_API_KEY=sk-your-api-key-here